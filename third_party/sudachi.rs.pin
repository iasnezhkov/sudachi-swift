# sudachi.rs source pin — read by scripts/fetch-sudachi-rs.sh.
#
# One full 40-character commit SHA; the first non-comment line wins. Bump it
# together with Cargo.lock and the CHANGELOG (see CONTRIBUTING.md, "Upstream
# awareness"). This replaces the old git submodule, which SwiftPM cloned into
# every consumer's checkout for no reason.
#
# 1e87493c = develop-v0.7 @ 2026-06-29 (unreleased; PR #357 merge commit).
# NOT a release tag — the 0.7 line only exists as a branch so far. Bump this to
# the real tag once upstream ships 0.7.
1e87493c9eabd8769433e6361ca3310a2c800cfe
